const CONFIG = {
    API_URL: "http://127.0.0.1:5000",        // frontend → backend (lokal)
    STREAM_URL: "http://127.0.0.1:5000",     // video stream (lokal)
    AI_URL: "https://plug-imply-overarch.ngrok-free.dev" // raspi → backend
};